# Returns success on substring match 
utils_has_string() {
    str="$1"
    sub="$2"
    if [ "${str#*"$sub"}" != "$str" ]; then
        return 0
    else
        return 1
    fi
}

# Transfers a file to client
utils_send_file() {
    echo "Content-Type: application/x-download"
    echo "Content-Disposition: attachment; filename=$2"
    echo ""
    
    cat $1/$2
}
